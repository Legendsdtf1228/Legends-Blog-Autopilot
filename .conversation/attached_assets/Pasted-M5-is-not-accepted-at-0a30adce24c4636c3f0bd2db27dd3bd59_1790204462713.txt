M5 is not accepted at 0a30adce24c4636c3f0bd2db27dd3bd59290be8d. Do not begin M6 or UI integration.

The implementation is M5-only and preserves M1–M4, but correct the following title-generation defects.

1. DISTINGUISH REAL QUESTIONS FROM GENERATED FRAMING

Legacy call sites currently generate a question with buildIntentReaderQuestion() and immediately pass it to buildNaturalTitle(). buildNaturalTitle() then treats it as a genuine reader question and converts it directly into the title.

Add explicit provenance, such as:

- ACTUAL_READER_QUESTION
- APPROVED_READER_TASK_QUESTION
- EDITORIAL_FALLBACK

Only actual or approved canonical ReaderTask questions may take the direct question-to-title path.

Generated editorial fallback questions must use the fallback title composer and must never masquerade as observed reader language.

Add tests proving:

- an actual ReaderTask question may directly drive a title;
- a generated fallback question cannot enter the direct-question path;
- generated comparison questions do not become awkward question-shaped titles;
- question provenance never changes evidence, demand, or approval status.

2. MAKE THE CANONICAL READERTASK DRIVE M5 OUTPUT

For active M1–M4 cluster paths, title and brief generation must use the canonical ReaderTask, including applicable:

- actualQuestion;
- decisionOrAction;
- situation;
- problem;
- audience;
- intent;
- desiredOutcome;
- constraints.

Do not merely declare these fields in the input type. Use them deterministically.

Keyword/format/intent fallback behavior may remain only for explicitly identified legacy paths. It must be labeled as editorial fallback and cannot claim ReaderTask provenance.

Add integration coverage showing that two ReaderTasks with the same keyword but different decisions, situations, audiences, or constraints produce appropriately different titles and briefs.

3. ENFORCE REAL TITLE-SHAPE DIVERSITY

Replace the current uniqueness-only test with a representative corpus test.

Normalize away:

- topic/keyword text;
- audience-specific nouns;
- punctuation;
- inconsequential singular/plural variation.

Then measure structural title patterns.

No single title structure may exceed 20% of the evaluation corpus. Do not achieve this by randomization. Output must remain deterministic and permutation-stable.

Also test for:

- grammatical agreement;
- awkward audience insertion;
- duplicated comparison wording;
- titles ending after unsafe clipping;
- deterministic output across identical retries.

4. RUN COMPLETE DATABASE-BACKED VERIFICATION

Start an isolated disposable PostgreSQL service in Replit.

Do not use production credentials or data.

Run:

- npm run typecheck
- complete npm test
- npm run build

All tests must reach their assertions. ECONNREFUSED is not an acceptable final M5 verification result.

5. PRESERVE ACCEPTED BOUNDARIES

Do not change:

- M1–M4 evidence or knowledge behavior;
- approval, freshness, authority, scope, contradiction, or SourceEvidence rules;
- AUTO_ELIGIBLE thresholds;
- decision routing;
- legacy opportunities or reservations;
- rollout qualification;
- production pause or draft-only defaults;
- database schema unless a correction absolutely requires an additive migration.

Update the M5 report accurately. Make one narrow correction commit on replit/m5 and return the full SHA. Stop after M5.